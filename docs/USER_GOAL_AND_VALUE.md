# User Goal and Business Value

## 1. User Goal

The primary user goal is simple:

> Allow a QA engineer to perform a desktop workflow once and reuse that workflow as automation instead of repeatedly performing the same actions manually.

---

## 2. Current Manual Process

A traditional workflow may look like:

```text
Open Application
      ↓
Enter Data
      ↓
Click Controls
      ↓
Navigate
      ↓
Validate Result
      ↓
Repeat During Regression